<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daily_todo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo_foreground"
        android:label="Daily Todo"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.daily_todo.MvvmMainActivity"/>
        <activity android:name="com.example.daily_todo.AddEditNoteActivity"/>
        <activity android:name=".fragment.FragmentActivity"/>
        <activity android:name=".mvvm.AdminLoginActivity" />
        <activity android:name=".RecyclerViewAdapter"/>
        <activity android:name=".MvvmActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".TodoDetailActivity" />
        <activity android:name=".AddTodoActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>